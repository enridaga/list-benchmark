prefix midi: <http://purl.org/midi-ld/midi#> 
prefix midi-note: <http://purl.org/midi-ld/notes/> 
prefix midi-prog: <http://purl.org/midi-ld/programs/> 
prefix prov: <http://www.w3.org/ns/prov#> 
prefix rdf: <http://www.w3.org/1999/02/22-rdf-syntax-ns#> 
prefix rdfs: <http://www.w3.org/2000/01/rdf-schema#> 
prefix xml: <http://www.w3.org/XML/1998/namespace> 
prefix xsd: <http://www.w3.org/2001/XMLSchema#> 

SELECT
	?track
	?event
	?attribute
	?value
FROM <list:benchmark:1k:list>
WHERE {
   ?track a midi:Track .
   ?track midi:hasEvents rdf:rest*/rdf:first ?event .
   ?event ?attribute ?value .
} # As per http://www.snee.com/bobdc.blog/2014/04/rdf-lists-and-sparql.html there might not be a way to retrieve these in order...
